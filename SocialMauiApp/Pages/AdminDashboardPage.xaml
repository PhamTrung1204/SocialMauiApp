<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SocialMauiApp.Pages.AdminDashboardPage"
             Title="AdminDashboardPage">
    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Tóm tắt -->
        <StackLayout Grid.Row="0" Margin="0,0,0,20">
            <Label Text="Tổng quan" FontSize="24" FontAttributes="Bold" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" 
                        Stroke="#CCCCCC" 
                        StrokeThickness="1" 
                        BackgroundColor="White"
                        StrokeShape="RoundRectangle 10"
                        Margin="5"
                        Padding="10">
                    <StackLayout>
                        <Label Text="Tổng bài viết" FontSize="16" />
                        <Label Text="{Binding TotalPosts}" FontSize="20" FontAttributes="Bold" />
                    </StackLayout>
                </Border>
                <Border Grid.Column="1" 
                        Stroke="#CCCCCC" 
                        StrokeThickness="1" 
                        BackgroundColor="White"
                        StrokeShape="RoundRectangle 10"
                        Margin="5"
                        Padding="10">
                    <StackLayout>
                        <Label Text="Bài viết đang chờ" FontSize="16" />
                        <Label Text="{Binding PendingPosts}" FontSize="20" FontAttributes="Bold" />
                    </StackLayout>
                </Border>
                <Border Grid.Column="2" 
                        Stroke="#CCCCCC" 
                        StrokeThickness="1" 
                        BackgroundColor="White"
                        StrokeShape="RoundRectangle 10"
                        Margin="5"
                        Padding="10">
                    <StackLayout>
                        <Label Text="Tổng người dùng" FontSize="16" />
                        <Label Text="{Binding TotalUsers}" FontSize="20" FontAttributes="Bold" />
                    </StackLayout>
                </Border>
            </Grid>
        </StackLayout>

        <!-- Nút điều hướng -->
        <Button Grid.Row="1" 
                Text="Quản lý bài viết" 
                Command="{Binding NavigateToPostManagementCommand}" 
                Margin="0,0,0,20" />

        <!-- Danh sách người dùng -->
        <CollectionView Grid.Row="2" 
                        ItemsSource="{Binding Users}" 
                        SelectedItem="{Binding SelectedUser, Mode=TwoWay}"
                        SelectionMode="Single">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackLayout>
                            <Label Text="{Binding Name}" FontSize="16" />
                            <Label Text="{Binding Email}" FontSize="14" FontAttributes="Italic" />
                            <Label Text="{Binding Role, StringFormat='Vai trò: {0}'}" FontSize="12" />
                            <Label Text="{Binding IsLocked, StringFormat='Trạng thái: {0:Đã khóa|Đang hoạt động}'}" FontSize="12" />
                        </StackLayout>
                        <BoxView Grid.Column="1" 
                                 Color="{Binding IsLocked, Converter={StaticResource BoolToColorConverter}}" 
                                 WidthRequest="20" 
                                 HeightRequest="20" 
                                 VerticalOptions="Center" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Nút hành động -->
        <StackLayout Grid.Row="3" Orientation="Horizontal" HorizontalOptions="End" Margin="0,10,0,0">
            <Button Text="Khóa/Mở khóa" 
                    Command="{Binding ToggleUserLockCommand}" 
                    IsEnabled="{Binding SelectedUser, Converter={StaticResource NullToBoolConverter}}" />
            <Button Text="Xóa người dùng" 
                    Command="{Binding DeleteUserCommand}" 
                    IsEnabled="{Binding SelectedUser, Converter={StaticResource NullToBoolConverter}}" />
        </StackLayout>
    </Grid>
</ContentPage>