<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SocialMauiApp.Pages.PreviewPhotoPage"
           BackgroundColor="Black"
             NavigationPage.HasNavigationBar="False">

    <Grid>
        <!-- Main Image with Tap Gesture -->
        <Grid>
            <Image x:Name="ProfileImage"
                   Aspect="AspectFit"
                   HorizontalOptions="Center"
                   VerticalOptions="Center">
                <Image.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnImageTapped" />
                </Image.GestureRecognizers>
            </Image>

            <ActivityIndicator x:Name="LoadingIndicator"
                              IsRunning="False"
                              IsVisible="False"
                              Color="White"
                              HorizontalOptions="Center"
                              VerticalOptions="Center"/>
        </Grid>

        <!-- Controls Overlay - Fades in/out when image tapped -->
        <Grid x:Name="ControlsOverlay" IsVisible="True">
            <!-- Semi-transparent Header Bar -->
            <Grid VerticalOptions="Start" HeightRequest="60" BackgroundColor="#44000000">
                <Button Text="✕" 
                        FontSize="20"
                        TextColor="White"
                        BackgroundColor="Transparent"
                        HorizontalOptions="Start"
                        Margin="15,0,0,0"
                        Clicked="OnCloseClicked"/>

                <Label Text="Profile Picture" 
                       TextColor="White"
                       FontSize="18"
                       FontAttributes="Bold"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"/>
            </Grid>

            <!-- Semi-transparent Footer Actions -->
            <Grid VerticalOptions="End" BackgroundColor="#44000000" Padding="20,15">
                <Button Text="Update Profile Picture"
                        BackgroundColor="#1877F2"
                        TextColor="White"
                        Clicked="OnUpdatePhotoClicked"
                        HeightRequest="45"
                        CornerRadius="8"/>
            </Grid>
        </Grid>
    </Grid>
</ContentPage>